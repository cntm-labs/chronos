name: Security Audit
on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]
  schedule:
    - cron: '0 0 * * 1'

jobs:
  
  audit:
    name: Dependency Audit
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable
      - name: Install cargo-audit
        run: cargo install cargo-audit
      - name: Run audit
        if: hashFiles('Cargo.toml') != ''
        run: cargo audit
  
  clippy-security:
    name: Static Analysis
    runs-on: ubuntu-latest
    permissions:
      contents: read
      security-events: write
    steps:
      - uses: actions/checkout@v4
      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          components: clippy
      - name: Install SARIF tools
        run: cargo install clippy-sarif sarif-fmt
      - name: Clippy SARIF
        if: hashFiles('Cargo.toml') != ''
        run: >
          cargo clippy --all-targets --all-features --message-format=json
          | clippy-sarif
          | tee results.sarif
          | sarif-fmt
        continue-on-error: true
      - name: Upload SARIF
        uses: github/codeql-action/upload-sarif@v3
        if: always()
        with:
          sarif_file: results.sarif

  
  mojo-security:
    name: Mojo Security Check
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Setup Modular CLI
        run: |
          curl -s https://get.modular.com | sh -
          modular auth ${{ secrets.MODULAR_AUTH_TOKEN }}
      - name: Install Mojo
        run: modular install mojo
      - name: Static Analysis
        if: hashFiles('**/*.mojo', '**/*.moj') != ''
        run: mojo check .

  
  lean-audit:
    name: Lean Dependency Audit
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Setup elan
        uses: leanprover/lean-action@v1
      - name: Check lake-manifest integrity
        if: hashFiles('lakefile.toml', 'lakefile.lean') != ''
        run: lake resolve-deps
      - name: Verify proof consistency
        if: hashFiles('lakefile.toml', 'lakefile.lean') != ''
        run: lake build
